import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "So here's our approach. Instead of one massive prompt, we decompose the problem into a set of agent roles. Each agent addresses a specific failure mode. The point is not more LLM calls. It's that each call has an auditable responsibility.",
];

export { narrations };
