import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Our method in one line: decompose into six agent roles — Parser, Planner, Retriever, Generator, Mapper — each followed by a Critic gate. One failure mode per step, fully auditable.",
];

export { narrations };
