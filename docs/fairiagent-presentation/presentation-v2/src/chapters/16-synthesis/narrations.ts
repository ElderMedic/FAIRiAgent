import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Let me be direct about where we stand. Phase-0 confirms the agentic workflow wins on ISA structure, evidence, and multi-row depth — but the headline numbers also expose real gaps.",
  "Four open challenges. First, only about thirty-one percent of Full runs complete end to end. Second, on row-aligned value accuracy, the zero-shot baseline still leads on the full eight-document benchmark. Third, no single metric is enough — structure and values must stay separate. Fourth, Phase-0 is N equals one with proxy ablations; we need Phase-1 and Phase-2 for statistical rigor.",
  "The path forward is engineering, not abandoning the design: improve completion, close the value gap, and rerun with factorial ablations and more repeats.",
];

export { narrations };
