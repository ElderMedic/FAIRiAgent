import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Materials in one sentence: eight manuscripts with curator gold ISA-Tab, five local models, four conditions from zero-shot to Full FAIRiAgent, one run per cell.",
  "Metrics stay dual-track. Track A is Metadata Schema — Hierarchical-F1 checks field name plus ISA position. Track B is Value Extraction — row-aligned accuracy checks value plus row binding.",
];

export { narrations };
