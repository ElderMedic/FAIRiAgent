import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Despite those challenges, FAIRiAgent already offers four concrete contributions worth keeping.",
  "First, an auditable multi-agent pipeline where each step has a named responsibility. Second, live FAIR-DS grounding so package names come from the registry, not imagination.",
  "Third, a dual-track evaluation that treats metadata schema and value extraction as separate tasks. Fourth, evidence-first drafts with ISA row binding — built for assisted curation, not blind submission.",
  "The design is directionally right on structure and provenance. The open work is making it complete and competitive on value copying.",
  "",
];

export { narrations };
