import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "The third experiment asks: if you give the system multiple attempts, how often does it succeed? This is Pass@k.",
  "The curve rises as k increases. But this is not random sampling. Each attempt learns from Critic feedback. For metadata curation, one lucky first answer matters less than reliable convergence to a reviewable object.",
];

export { narrations };
