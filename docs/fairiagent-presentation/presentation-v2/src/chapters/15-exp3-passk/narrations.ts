import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "The third experiment asks: if you give the system multiple attempts, how often does it succeed? This is Pass@k — preliminary, N equals one per cell in Phase-0.",
  "The curve rises as k increases. Each attempt learns from Critic feedback, not random resampling. For curation, reliable convergence to a reviewable object matters more than a lucky first answer.",
];

export { narrations };
