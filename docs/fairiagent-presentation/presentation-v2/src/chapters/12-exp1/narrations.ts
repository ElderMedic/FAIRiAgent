import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Results, briefly. On biosensor plus earthworm — where Full completes — Hierarchical-F1 is zero point seven two one, ahead of baselines.",
  "Structural win on this slice; value gap on the full benchmark comes next under challenges.",
];

export { narrations };
