import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Do you really need all these pieces? This slide uses proxy run-quality metrics from completed Phase-0 cells — not a full factorial ablation yet.",
  "The pattern still matters: removing the critic lowers scores and raises hallucination risk; removing rollback hurts more. Both mechanisms are doing real work in the workflow.",
];

export { narrations };
