import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "One more challenge: context degradation. By the time the Value Mapper runs, the system may have lost track of early discoveries. Session memory preserves working context across steps.",
  "In our experiments, memory reduces runtime for local models and improves or maintains metadata breadth. It's especially critical for smaller context windows.",
];

export { narrations };
