import type { Narration } from "../../registry/types";

const narrations: Narration[] = [
  "Session memory keeps evidence, packages, and uncertain fields across steps — critical for local models with smaller context windows.",
  "In Phase-0, memory cuts runtime without shrinking metadata breadth.",
];

export { narrations };
